{
  "name": "devstats",
  "private": true,
  "version": "0.1.0",
  "description": "Custom GitHub stats cards powered by Vercel SVG API",
  "license": "MIT",
  "type": "commonjs",
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node -e \"console.log('Run Vercel Dev: npx vc@latest dev -A vercel.local.json --listen 3000')\"",
    "dev:preview": "tsx watch dev.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@vercel/node": "^3.2.27",
    "tsx": "^4.20.5",
    "typescript": "^5.8.3"
  }
}
